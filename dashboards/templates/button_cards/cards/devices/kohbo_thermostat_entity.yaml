kohbo_thermostat_entity:
  template: kohbo_device_entity
  icon: >
    [[[
      if (entity.attributes.preset_mode === 'eco')
        return "mdi:leaf"
      else if (entity.state == "heat_cool")
        return "mdi:sun-snowflake-variant"
      else if (entity.state == "heat")
        return "mdi:heat-wave"
      else if (entity.state == "cool")
        return "mdi:snowflake"
    ]]]
  state_display: >
    [[[
      if (entity.attributes.preset_mode === 'eco')
        return "Eco Mode"
      else if (entity.state == "heat_cool")
        return `Heat/Cool: ${entity.attributes.target_temp_low} - ${entity.attributes.target_temp_high}`
      else if (entity.state == "heat")
        return `Heat – ${entity.attributes.temperature}`
      else if (entity.state == "cool")
        return `Cool – ${entity.attributes.temperature}`
      else
        return "Unknown"
    ]]]
  styles:
    icon:
      - color: >
          [[[
            if (entity.attributes.preset_mode === 'eco')
              return "var(--success-color)";
            else if (entity.state == "heat_cool")
              return "var(--state-climate-heat_cool-color)";
            else if (entity.state == "heat")
              return "var(--state-climate-heat-color)";
            else if (entity.state == "cool")
              return "var(--state-climate-cool-color)";
          ]]]




  # <template TemplateState(<state climate.nest_main_floor=heat_cool; hvac_modes=[<HVACMode.HEAT: 'heat'>,
  # <HVACMode.COOL: 'cool'>,
  # <HVACMode.HEAT_COOL: 'heat_cool'>,
  # <HVACMode.OFF: 'off'>],
  # min_temp=50,
  # max_temp=90,
  # fan_modes=['on',
  # 'off'],
  # preset_modes=['none',
  # 'eco'],
  # current_temperature=74,
  # temperature=None,
  # target_temp_high=77,
  # target_temp_low=72,
  # current_humidity=47.0,
  # fan_mode=off,
  # hvac_action=idle,
  # preset_mode=none,
  # friendly_name=Nest Main Floor,
  # supported_features=411 @ 2025-10-16T13:11:07.555720-05:00>)>