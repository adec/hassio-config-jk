template:
  - sensor:
      - name: "Main Bedroom People List"
        unique_id: main_bedroom_people_list
        state: >
          {% set people = expand('group.people_sensors') %}
          {% set in_room = people | selectattr('attributes.room_presence', 'equalto', 'main_bedroom') | map(attribute='entity_id') | list %}
          {{ in_room | length }}
        attributes:
          people: >
            {% set people = expand('group.people_sensors') %}
            {% set in_room = people | selectattr('attributes.room_presence', 'equalto', 'main_bedroom') | map(attribute='entity_id') | list %}
            {{ in_room }}